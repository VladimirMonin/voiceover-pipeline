[project]
name = "voiceover-pipeline"
version = "0.5.1"
description = "Standalone chunked TTS voiceover + Whisper timing CLI"
readme = "README.md"
license = {text = "MIT"}
license-files = ["LICENSE"]
authors = [
  {name = "Vladimir Monin", email = "moninvladimir88@gmail.com"},
]
keywords = [
  "tts",
  "text-to-speech",
  "voiceover",
  "whisper",
  "remotion",
  "podcast",
  "audio",
  "subtitles",
  "srt",
  "cli",
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Multimedia :: Sound/Audio :: Speech",
  "Topic :: Utilities",
]
requires-python = ">=3.11"
dependencies = [
  "openai>=2.32.0",
  "requests>=2.33.0",
]

[project.optional-dependencies]
timing-whisper = [
  "faster-whisper>=1.0.0",
  "ctranslate2>=4.6.2",
]
voiceover-qwen = [
  "torch>=2.0.0",
  "qwen-tts>=0.1.0",
  "soundfile>=0.12.0",
  "numpy>=1.24.0",
]
asr-qwen = [
  "qwen-asr",
]
cuda = [
  "nvidia-cudnn-cu12>=9.0.0; sys_platform == 'win32' or sys_platform == 'linux'",
  "nvidia-cublas-cu12>=12.0.0; sys_platform == 'win32' or sys_platform == 'linux'",
]
asr-nemotron = [
  "accelerate>=1.0.0",
  "librosa>=0.10.0",
  "torch>=2.0.0",
  "transformers>=5.0.0",
]

[project.urls]
Homepage = "https://github.com/VladimirMonin/voiceover-pipeline"
Repository = "https://github.com/VladimirMonin/voiceover-pipeline"
Issues = "https://github.com/VladimirMonin/voiceover-pipeline/issues"

[project.scripts]
voiceover = "voiceover_pipeline.cli:main"
voiceover-pipeline = "voiceover_pipeline.cli:main"

[dependency-groups]
dev = [
  "mypy>=2.3.1",
  "pytest>=8.0.0",
  "ruff>=0.16.3",
]

[tool.uv]
package = true
conflicts = [
  [{ extra = "asr-qwen" }, { extra = "voiceover-qwen" }],
  [{ extra = "asr-qwen" }, { extra = "asr-nemotron" }],
  [{ extra = "asr-nemotron" }, { extra = "voiceover-qwen" }],
]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.mypy]
python_version = "3.11"
files = ["src/voiceover_pipeline"]
ignore_missing_imports = true
check_untyped_defs = true
warn_redundant_casts = true
warn_unused_ignores = true
show_error_codes = true

[tool.hatch.build.targets.wheel]
packages = ["src/voiceover_pipeline"]

[tool.hatch.build.targets.sdist]
include = [
  "/src",
  "/tests",
  "/in",
  "/docs",
  "/pyproject.toml",
  "/README.md",
  "/LICENSE",
  "/CHANGELOG.md",
  "/uv.lock",
  "/.gitignore",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

---
title: "Журнал разработки — Voiceover Pipeline — 2026-08-17"
kind: daily-project-retrospective
project: "voiceover-pipeline"
date: 2026-08-17
timezone: "+05:00"
created_at: "2026-08-17T06:38:48+05:00"
updated_at: "2026-08-17T11:02:56+05:00"
previous_report: "2026-08-16.md"
baseline_commit: "0c12ba1275830908b19b9e55354478091e7b167c"
head_commit: "8bea52023ffafddc91b205bfd37f0c1d650786c7"
status: active
---

# Журнал разработки — Voiceover Pipeline — 2026-08-17

## Короткий итог

После реального клонирования голоса Qwen3-TTS пользователь оценил результат как
«Голос неплох прямо», отложил дополнительные образцы и перенёс приоритет на
Qwen/Nemotron ASR и числительные. Старый Qwen full-file запуск на 6:28 вернул
ложный `success`: обработал только начало и не дал таймингов. Новый
незакоммиченный long-form слой уже провёл Qwen через весь источник четырьмя
чанками, но это только доказательство полного покрытия: WER остаётся `0.27056`,
есть лишь coarse chunk segments, word timings отсутствуют. Product tree по-прежнему
смешанный и dirty; ни текущий long-form код, ни Qwen/Nemotron provider bytes не
представлены отдельным принятым commit.

## Что изменилось сегодня

- **USER DECISION:** клонирование Qwen3-TTS достаточно перспективно для
  продолжения с новыми образцами позже; текущий приоритет — улучшение
  распознавания Qwen и Nemotron, отдельно числительные.
- **EXECUTED FACT:** механические и content-проверки clone-артефакта прошли;
  пользовательская слуховая оценка получена отдельно и не превращает один
  образец в общий quality PASS.
- **EXECUTED FACT:** baseline Qwen на источнике `387.96 s` завершился с кодом
  `0`, но покрыл только `27.652%` исходного endpoint, вернул 212 слов, ноль
  сегментов и ноль word timings.
- **EXECUTED FACT:** свежий Qwen long-form run покрыл `387.96 s` полностью:
  четыре чанка, `coverage_verified: true`, endpoint fraction `1.0`, 796 слов
  против 839 в reference, WER `0.27056`, wall time `113.0816 s`.
- **EXECUTED FACT:** добавлен 12-case дизайн проверки русских числительных с
  раздельными raw и semantic metrics; аудио и результаты ASR ещё отсутствуют.
- **NOT VERIFIED:** новый Nemotron live inference/timing parity сегодня не
  доказан прямым артефактом.

## Решения пользователя

- **USER DECISION:** «Голос неплох прямо. Я потом другие образцы пришлю».
- **USER DECISION:** «Давай работать над улучшением распознавания квен и
  немотрон. Ну и числительные».
- **USER DECISION:** после преждевременной остановки работы пользователь явно
  потребовал продолжить, а не ждать дополнительного разрешения.
- Push, tag, release и новый GPU/model inference в рамках этой ретроспективы не
  разрешались и не выполнялись.

## Выполнено и проверено

### Qwen3-TTS cloning

- **EXECUTED FACT:** QA receipt фиксирует Base/clone route, выходной MP3
  `11.904 s`, 24 kHz mono, без clipping, NaN и длинной тишины; content-check
  получил WER `0.125` без обнаруженного пропуска фразы.
- **USER DECISION:** пользовательская перцептивная оценка — положительная, но
  ограниченная одним Telegram sample; дополнительные samples будут позже.
- **NOT VERIFIED:** устойчивость сходства на других образцах, длинном тексте,
  числительных и границах чанков.

### Qwen ASR long-form

- **SUPERSEDED:** старый `status: success` на whole-file route нельзя считать
  полным результатом. Прямой baseline JSON подтверждает truncation: 1535
  символов, 212 слов, endpoint `0.27652`, `duration_s: null`,
  `timestamp_mode: none`, без segments/words.
- **EXECUTED FACT:** fixed JSON содержит 5826 символов, 796 слов, четыре
  `chunked` segments и покрытие полного tail до `387.96 s`; последний чанк
  присутствует явно.
- **NOT VERIFIED:** `success` нового run не равен semantic PASS. Word timings
  отсутствуют, три tail-marker проверки false, а WER `0.27056` требует
  отдельного quality verdict.

### Nemotron и числительные

- **EXECUTED FACT:** ранее принятый offline Nemotron contract и GPU preflight
  остаются историческим evidence; тот live gate не запускал model inference.
- **NOT VERIFIED:** новый Nemotron long-form run, native timing parity,
  50-case benchmark, cancellation и unload.
- **EXECUTED FACT:** numeral manifest задаёт 12 кейсов и не перезаписывает raw
  ASR text при semantic normalization.
- **NOT VERIFIED:** manifest имеет `audio_status:
  pending_independent_recording_or_tts_route`; ASR scores отсутствуют.

## Текущая работа

- Существенный handoff-якорь long-form ветки — Kanban `t_8ed3d9f4`.
  **NOT VERIFIED:** live `show/runs/comments` недоступны в текущем delegated
  read-only context: Hermes CLI отказал при инициализации Kanban. Поэтому статус
  карточки, semantic verdict и возможный reclaimed review здесь не угаданы.
- Возможный reclaim control-plane review из-за lifecycle mismatch сам по себе
  не назван product blocker: без live read-back это только **AGENT CLAIM / NOT
  VERIFIED**, а product evidence оценивается по прямым JSON и exact bytes.
- `src/voiceover_pipeline/asr_longform.py`, связанные providers/tests/docs и
  другие product-файлы находятся в dirty tree. Это **не commit** и не общий
  PASS; отдельного acceptance digest текущего среза нет.

## Реальные проблемы

1. Qwen whole-file route раньше выдавал ложный `success` при фактической
   truncation; fixed run закрывает coverage defect, но не доказывает качество
   текста или word alignment.
2. Nemotron live/timing evidence и числительные ещё не получены.
3. Рабочее дерево крупно и смешанно; scoped staging обязателен, `git add .`
   запрещён.
4. Live Kanban не читается из delegated context, поэтому control-plane verdict
   нельзя подменять предположением.

## Планы и архитектурные документы

- [Audio.cpp hybrid local runtime migration plan](../plans/2026-08-16-audio-cpp-hybrid-migration-plan.md)
  — активный канонический план; часть status-текста отстаёт от dirty implementation.
- [Generic local ASR implementation plan](../plans/2026-08-15-generic-local-asr-implementation-plan.md)
  — активный источник offline-first и benchmark gates.
- [Pinned local audio runtime contract](../audio-cpp-runtime.md) — активный
  runtime/platform boundary.
- [Agent CLI contract](../agent-cli-contract.md) — текущий dirty контракт
  long-form orchestration; `chunked` означает coarse spans, а не native/forced
  word alignment.

## Коммиты и проверенные артефакты

| Anchor | Что доказывает | Состояние |
|---|---|---|
| `8bea52023ffafddc91b205bfd37f0c1d650786c7` | Документация локальных audio.cpp models | `HEAD`, local-only |
| `2790889b2fb64aa2dc35f81b060b6018e4452745` | Первая версия отчёта 2026-08-17 | local-only, заменена этим обновлением |
| `c46766a35b1b10b7955e6f961e68c7f8d3c12b3c` | Канонический migration plan | `origin/main` |
| `f2bf0a4f1d62bf3f6768febd7fd08b8839b5dc59700d8ec708dafed9d16def3b` | Exact Telegram reference для clone receipt | private artifact digest |
| `83fbc3c20f8f1f3ab4858918639deaf8018fa5fcab388fb027f15d4d37c70f64` | Exact blind clone output | private artifact digest |
| `abdc5208983e09badeaafc90c8449d1f052599a66e462a5be56c698eb57a8ff1` | Ранее принятый Nemotron offline slice | dirty-tree manifest, не commit |

За сутки есть два документационных commit до этого обновления. Ни один из них
не коммитит текущий long-form/provider implementation. `main` до нового
ретроспективного commit опережает `origin/main` на четыре commit; push не
выполнялся.

## Важные цитаты

> **Владимир:** «Голос неплох прямо. Я потом другие образцы пришлю. Давай
> работать над улучшением распознавания квен и немотрон. Ну и числительные».

Источник: `2026-08-17T07:01:38+05:00`, session
`default/20260702_122854_b35900a3`, message `506798`.

> **Владимир:** «Продолжай. Боже мне тебя упрашивать теперь работу доделать?»

Источник: `2026-08-17T07:29:06+05:00`, та же session, message `507253`.

## Ошибки и отменённые решения

- **SUPERSEDED:** прежний Qwen full-file `success` не является полным
  распознаванием; прямой artifact доказывает частичный output.
- **SUPERSEDED:** ожидание новой команды после уже поставленного ASR приоритета
  было неверным; пользователь потребовал продолжать.
- **SUPERSEDED:** утверждение первой версии отчёта «новых commits сегодня нет»
  устарело: созданы два docs commits, но product code всё ещё не закоммичен.
- **SUPERSEDED:** `WVM_ROOT unset` не считается инфраструктурным blocker
  Nemotron; канонический checkout ранее был найден.
- **NOT VERIFIED:** lifecycle reclaim возможного control-plane review не
  интерпретируется ни как PASS, ни как product blocker без live run/comments.

## Следующий шаг

1. В owner/root context выполнить read-only `show/runs/comments` для
   `t_8ed3d9f4` и дать semantic verdict exact fixed Qwen JSON и соответствующим
   dirty bytes (coverage, качество текста, coarse timings, отсутствие word
   timings) без нового model inference; только после этого переходить к
   Nemotron и numeral live gates.

## Не проверено

- Live state, runs и comments Kanban `t_8ed3d9f4` в момент отчёта.
- Semantic acceptance нового Qwen long-form JSON и exact dirty-code manifest.
- Qwen forced word timings/SRT на long-form source.
- Nemotron long-form/native timing live parity и 50-case benchmark.
- Распознавание 12 numeral cases и semantic numeric scores.
- Clone quality на новых пользовательских samples.
- Общий integration commit, push, tag или release product bytes.

## Источники

- Project router: `AGENTS.md`; core/provider/test/docs/Kanban/Git instructions.
- Session: `default/20260702_122854_b35900a3`, messages `506798`, `507252`,
  `507253`; compaction summary не использовался как первоисточник.
- Direct baseline:
  `/media/v/storage/voiceover-pipeline/asr-longform-baseline/qwen/full-file.jsonl`,
  `full-file.coverage.json`, `full-file.timing.json`.
- Direct fixed run:
  `/media/v/storage/voiceover-pipeline/asr-longform-fixed/qwen/full-file.jsonl`,
  `full-file.coverage.json`, `full-file.timing.json`.
- Private clone QA receipt:
  `/media/v/storage/voiceover-pipeline/private-voice-samples/vladimir/final/vladimir-qwen3-tts-clone-blind.qa.json`;
  private source/output text не переносился в отчёт.
- Numerals:
  `/media/v/storage/voiceover-pipeline/asr-numerals/russian-numerals-manifest.json`.
- Git exact status/log read-back: `2026-08-17T10:57:33+05:00`.
- Независимая read-only reconstruction: Codex CLI, завершена до `11:02`; её
  вывод сверен с direct artifacts/session/Git. Kanban в обоих delegated
  contexts был read-restricted.

## Обновления документа

- `06:38` — первый отчёт: OmniVoice/Windows/Qwen/Nemotron и Qwen3-TTS crash tail.
- `11:02` — добавлены owner verdict по clone, новый приоритет ASR/числительных,
  baseline false-success, прямой fixed Qwen long-form result, dirty-tree и
  Kanban limitations; anchors дедуплицированы.
